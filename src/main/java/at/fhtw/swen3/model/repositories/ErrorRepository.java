package at.fhtw.swen3.model.repositories;

//import org.springframework.data.jpa.repository.JpaRepository;


public interface ErrorRepository /*extends JpaRepository<Error, Integer>*/ {
    //List<Error> findAll();
    //Error findByText(String text);
}
